---
title: scale-fill-viridis-b
subtitle: Binned viridis fill scale.
category: Scales
since: 0.1.0
engine: markdown
---


Partitions the continuous domain into `n-breaks` equal segments, each
coloured from the chosen viridis palette.

## Usage

```typst
scale-fill-viridis-b(
  option: "viridis",
  n-breaks: 5,
  name: none,
  limits: none,
  labels: auto,
)
```


## Parameters

| Parameter | Default | Description |
| --- | --- | --- |
| `option` | `"viridis"` | Palette name: `"viridis"`, `"magma"`, `"plasma"`, `"inferno"`, or `"cividis"`. |
| `n-breaks` | `5` | Number of bins to partition the domain into. |
| `name` | `none` | Legend title. Overrides any name set via [`labs`](../labs/labs.qmd) when both are present. |
| `limits` | `none` | Pair `(lo, hi)` clipping the trained domain, or `none`. |
| `labels` | `auto` | Array of legend labels aligned with the bins, or `auto`. |

## Returns

Scale object consumed by [`plot`](../core/plot.qmd).

## Examples

```{typst}
//| output-filename: "/assets/typst-render/scale-fill-viridis-b-1.svg"
//| width: 10cm
//| height: 6cm
#let d = range(0, 12).map(i => (grp: str(i), y: i + 1))
#plot(
  data: d,
  mapping: aes(x: "grp", y: "y", fill: "y"),
  layers: (geom-col(),),
  scales: (scale-fill-viridis-b(n-breaks: 4),),
)
```

## See also

[`scale-fill-viridis-c`](scale-fill-viridis-c.qmd), [`scale-fill-viridis-d`](scale-fill-viridis-d.qmd), [`scale-colour-viridis-b`](scale-colour-viridis-b.qmd).
