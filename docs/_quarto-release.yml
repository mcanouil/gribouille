# Used by the tagged-release docs build.
# feature-matrix.qmd is a development-only coverage table; keep it off the public site.
project:
  render:
    - "*.qmd"
    - "**/*.qmd"
    - "!feature-matrix.qmd"
